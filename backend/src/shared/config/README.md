# 🏗 Shared - Config

## Responsabilité
- Environment variables & configuration
- Constants (roles, permissions)
- Global app settings

## Fichiers
- `env.ts` ← Environment validation (Zod schema)
- `app.ts` ← App configuration (port, origins, etc.)
- `database.ts` ← Database configuration
