# 🏠 Domain - Servers

## Responsabilité
- Types métier pour les serveurs
- Erreurs spécifiques au domaine serveurs

## Fichiers
- `types.ts` : Server, ServerMember, UserRole, Channel, CreateServerRequest, etc
- `errors.ts` : ServerError, ServerNotFoundError, ServerAccessDeniedError

## Règles
-  Aucune dépendance externe
-  Enums en UPPER_CASE
-  Types purs TypeScript
